# 目前開發階段（給 Agent 與協作者）

請將下方 **階段** 的數字改為 `DEVELOPMENT-WORKFLOW.md`「階段總覽表」中，**正要請 Agent 實作**的那一列編號（**0**～**12**）。

**階段：`12`（流程 0～12 已落地；維運請見 `DEPLOYMENT.md` 與 README）**

---

- 驗收通過、要進入下一階段時：把此數字改成下一號並存檔即可；**不必**每輪複製 Prompt（專案已啟用 Cursor `alwaysApply` 規則）。
- 若你在對話裡**明確寫了**其他階段號（例如「請先做階段 3」），Agent 以對話為準。
