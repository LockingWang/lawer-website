# 無憂吳律事務所｜網站操作流程、切版、風格與 UX 規劃

本文為網站開發前的共識文件，供討論修訂後定稿，再進入設計稿與前端實作。

---

## 一、網站定位與目標使用者

| 項目 | 說明 |
|------|------|
| **品牌** | 無憂吳律事務所（Sallywu Attorneys-At-Law） |
| **核心訊息** | 專業可信、溫暖承接憂慮、「來到本所後，從此無憂無慮」 |
| **主要訪客** | 需要家事／訴訟／校園與行政事件諮詢的民眾；搜尋在地律師者 |
| **次要訪客** | 合作單位、媒體、欲邀請演講或委員身分之機關 |

**網站首要目標**：讓訪客在 30 秒內理解「這誰、做什麼、怎麼聯絡」，並降低撥打電話或填表前的焦慮。

### 1.1 與律師討論已定之功能與內容（需求清單）

以下為已確認要製作的項目，後續開發與切版皆應涵蓋。

| 編號 | 項目 | 說明 |
|------|------|------|
| 1 | **本所介紹** | 獨立頁面呈現事務所理念、特色與服務範疇；可與首頁摘要互相連結。 |
| 2 | **留言／表單寄信** | 使用者填寫留言後，由 **Railway 後端 API** 寄至律師指定信箱（見 §2.5、§6.5、**§8**）。表單主要置於 **聯繫本所** 頁。 |
| 3 | **律師介紹** | 獨立頁面：照片、學經歷、專長、現職與證照等（細節依 `intruduction.md` 定稿）。 |
| 4 | **最新消息** | 列表與單篇（公告、活動、文章摘要）；**內容一律於前端專案維護**（見 **§8.3**），利於 SEO 與律所動態曝光。 |
| 5 | **限時動態（串接 Instagram）** | 於網站展示與 **IG 限時動態** 連動之區塊；資料由 **同一後端** 向 Meta 取得（見 §2.6、**§8**）。 |
| 6 | **聯繫本所** | 整合 **留言表單**、電話／地址／Email／地圖，並提供 **Facebook、Instagram、LINE** 等社群連結；**頁尾另附 LINE QR Code**（見 §2.2b）。 |

---

## 二、操作流程規劃（使用者動線）

### 2.0 響應式網頁（RWD）為必備條件

本網站 **一律採行動優先（Mobile-first）思維**：設計與切版須先確保 **手機版可讀、可點、好捲動**，再擴展至平板與桌面。**不得**只做桌面版後勉強縮放。斷點與版面細節見 **§3.3**；全站 **頂部導覽列** 與 **頁尾** 規格見 **§2.2b**。

### 2.1 典型情境與路徑

1. **急於諮詢者**  
   進站 → 首屏即見電話／地址／「聯繫本所」→ 撥號或填表（最短路徑）。

2. **先了解專業領域者**  
   進站 → 本所介紹／服務摘要 → 律師介紹 → 聯繫本所。

3. **搜尋特定關鍵字者**（如家事、高雄律師）  
   著陸頁或首頁對應區塊 → 建立信任（學經歷、理念）→ CTA。

4. **僅需地址或傳真者**  
   頁尾或聯絡頁固定呈現，避免藏於多層選單。

### 2.2 建議資訊架構（IA）

依目前共識，採用下列結構（導覽列建議順序可再微調）：

```
首頁（精選：理念一句、服務摘要、最新消息精選、限時動態預覽、聯絡 CTA）
├── 本所介紹
├── 律師介紹
├── 最新消息（列表 → 單篇詳情）
├── 聯繫本所（留言表單、電話／地址／地圖／信箱；FB／IG／LINE 連結）
└── （選）隱私權政策／網站免責（表單與個資法建議備置）
```

**首頁與內頁分工**：「本所介紹」承載完整論述；首頁僅節錄引導至內頁。「留言寄信」**集中於聯繫本所頁**（全站頁尾可再放次要連結至該頁表單區）。

### 2.2b 全站框架：頂部導覽列（LOGO）與頁尾（Footer）

下列為全站固定區域之 **已定規格**，實作時須於 **各 RWD 斷點** 維持資訊一致、連結目標一致。

**頂部（Header）**

- **置頂區域**：左側或中央為 **事務所 LOGO**（連結至首頁）；旁為 **主要導覽連結**（與 §2.2 IA 一致：本所介紹、律師介紹、最新消息、聯繫本所等）。  
- **手機版**：導覽收合為 **漢堡選單**（抽屜／全幅選單皆可）；展開後連結與桌面版 **同一組路由**，方便使用者於小螢幕完成動線。  
- **可選**：向下捲動時縮小高度或固定頂欄（見 §4.5），但勿遮擋內文操作。

**底部（Footer）**

- **聯絡資訊**：電話（可點擊撥號）、地址、Email、傳真（若有）等，文字須與 **聯繫本所頁／`intruduction.md`** 一致，避免多版本糾紛。  
- **網站地圖式連結**：提供與 **頂部導覽列相同之一級頁面連結**（首頁、本所介紹、律師介紹、最新消息、聯繫本所等），點擊後 **導向對應頁面**（與 Header 使用相同 URL／路由）。  
- **社群連結**：Facebook、Instagram、LINE 等 **圖示＋可辨識文字**；外連建議新分頁開啟並補 `rel="noopener noreferrer"`。  
- **LINE QR Code**：頁尾放置 **官方 LINE 加入好友／聯絡用 QR Code 圖檔**（由事務所提供）；旁附簡短說明（例如「掃描加入官方 LINE」）。圖檔需 `alt` 文字；**手機版**上 QR 寬度勿過小（建議最短邊不少於約 **120px～160px** 視覺尺寸，且檔案清晰），必要時可另附「開啟 LINE 連結」按鈕作為第二管道。

**RWD 備註**：Footer 內容較多時，手機版宜改為 **直向堆疊**（聯絡資訊 → 導覽連結 → 社群 → LINE QR），避免擠成難點擊的橫排；桌面版可雙欄或多欄排版。

### 2.3 每頁最低限度內容

| 頁面 | 必備元素 |
|------|----------|
| 全站 | **RWD**（§2.0、§3.3）；**頂部**：LOGO＋主導覽（手機為漢堡選單）；**底部**：聯絡資訊、與頂部相同之一級連結、社群、**LINE QR Code**（§2.2b）；行動裝置可選固定聯絡捷徑 |
| 首頁 | 價值主張、本所／服務摘要、最新消息精選（2～3 則）、IG 限時動態區塊、明確 CTA 至聯繫頁 |
| 本所介紹 | 創所理念、服務領域概述、與首頁文案呼應；引導至律師介紹與聯繫 |
| 律師介紹 | 照片、學經歷、專長、現職、證照；聯絡 CTA |
| 最新消息 | 列表（日期、標題、摘要）；詳情頁可分享／回到列表 |
| 聯繫本所 | **留言表單（送出後寄信至律師信箱）**、電話可點擊、地址＋地圖、Email／傳真；**FB、IG、LINE** 外連 |

**專業服務**：若與「本所介紹」合併可維持一頁；若資訊量多，可另拆「服務項目」子頁，避免導覽過載。

### 2.4 轉換流程（Conversion）

- **主要轉換**：來電、留言表單（寄信）、點擊 LINE／Email。  
- **設計原則**：每一屏至少一個清楚次級動作（「了解更多」「前往聯繫」）；聯繫頁為表單與社群之 **完整入口**，頁尾可重複次要連結。

### 2.5 留言功能：表單送出後寄信至律師信箱

**目標**：訪客送出留言後，律師於指定 Email 收到同一封通知信（可含表單欄位內容）。

**本專案定案**：表單經 **Railway 上同一後端專案** 接收 POST，再以 SMTP 或交易型郵件 API 寄出（細節見 **§8**）。

**其他常見實作方式**（參考用，本專案不採用除非另有變更）：

| 方式 | 說明 |
|------|------|
| **後端 API + SMTP** | 自建小服務接表單 POST，以事務所 SMTP 或交易型郵件服務寄出；需防機器人與速率限制。**← 本專案即為此路線（後端位於 Railway）。** |
| **第三方表單／自動化** | 如 Formspree、Getform、Google Apps Script、Zapier／Make 等轉寄信箱；建置快，需確認隱私條款與資料跨境。 |
| **CMS 外掛** | 若使用 WordPress 等，可用 Contact Form + SMTP 外掛。 |

**建議表單欄位**：姓名、聯絡電話或 Email（至少擇一必填）、諮詢摘要、勾選「已閱讀隱私權聲明」、送出按鈕。  
**法遵與資安**：於聯繫頁或頁尾連結 **隱私權政策**（個資蒐集目的、保存期間）；加上 **防機器人**（例如 honeypot、reCAPTCHA、或後端限流）。  
**備援**：表單失敗時顯示「請改撥電話或寄 Email」，避免訪客無路可走。

### 2.6 Instagram 限時動態串接（技術與產品備註）

Meta 官方 **Instagram Graph API** 可取得「目前有效的限時動態」，但通常需：

- Instagram 帳號為 **專業帳戶**（企業／創作者），且與 **Facebook 粉絲專頁** 綁定。  
- 建立 **Meta 開發者應用程式**，完成權限審核與權杖管理。  
- 限時動態 **24 小時後即失效**，網站端需 **定期向 API 重新抓取** 並快取，否則區塊會空白。

**若 API 條件尚未齊備**，可採過渡方案：

- 首頁區塊顯示「追蹤 IG」＋大按鈕 **開啟 Instagram 個人檔案／限時動態**（行動裝置可試用 `instagram://` 或官方連結）。  
- 或改嵌入 **IG 貼文／影音精選**（與限時動態不同，但實作較單純），並於文案說明「即時動態請至 IG 觀看」。

實作前請確認：是否已有 **企業／創作者 IG**、是否願意維護 **Meta 開發者設定**；再決定「全自動串接」或「導流至 IG App」。

**本專案定案**：限時動態資料由 **Railway 後端** 代理 Instagram Graph API（權杖與 Secret 僅存後端）；前端僅呼叫後端公開之 JSON API；若 API 尚未就緒或失敗，仍依上列過渡方案顯示（見 **§8**）。

---

## 三、如何切版（設計稿 → 前端結構）

### 3.1 切版意義（本專案採用方式）

將設計稿拆解為 **可重複使用的區塊（sections）與元件（components）**，並對應到 HTML/CSS 結構，便於 RWD 與後續改版。

### 3.2 建議區塊拆解（以首頁為例）

| 區塊 | 內容概要 | 切版注意 |
|------|----------|----------|
| Header | **LOGO**（連回首頁）、主導覽；手機為 **漢堡選單** | 全寬置頂；觸控區 ≥ 44px；與 Footer 連結 **同一套路由** |
| Hero | 主標、副標、主視覺、主 CTA | 一句話可讀完；圖片需 alt；手機避免過高 Hero 佔滿屏 |
| 信任列 | 法扶／現職／證照等短標 | 可橫向捲動或小卡網格 |
| 服務摘要 | 3～6 項專業領域 | 連結至本所介紹或服務錨點 |
| 理念／差異 | 「無憂無慮」與創所理念 | 文字可掃讀；連結至「本所介紹」 |
| 最新消息精選 | 2～3 則標題＋日期 | 連結至「最新消息」列表或單篇 |
| IG 限時動態 | 橫向故事列或輪播 | 載入失敗時顯示「前往 IG」備援 |
| 主持律師預覽 | 照片＋一句話＋連結 | 與「律師介紹」內頁一致 |
| CTA | 電話、「聯繫本所」連結 | 對比足夠、按鈕夠大 |
| Footer | 聯絡方式、**與 Header 相同之一級連結**、社群、**LINE QR** | 與 §2.2b 一致；手機直向堆疊；hover／focus 動效見 §4.5 |

### 3.2b 動效相關切版備註

- 設計稿宜標註 **哪些區塊做捲動進場**、**卡片 hover 規格**，以利前端一致實作。  
- 若有 **輪播／限動預覽區**，需設計 **暫停、上一則／下一則** 狀態；並預留 **減少動態模式** 下的靜態版面。

### 3.3 RWD 斷點建議（可於定稿時微調）

**本專案以手機版為設計必備前提**：版型須於下列斷點皆可正常瀏覽與操作；**Header／Footer** 於各寬度皆須完整呈現（見 §2.2b）。

| 名稱 | 大致寬度 | 排版重點 |
|------|----------|----------|
| Mobile | 小於 768px | 單欄、**漢堡選單**、拇指可及的 CTA；Footer **直向堆疊**，LINE QR 尺寸足夠掃描 |
| Tablet | 768px～1023px | 可雙欄；導覽可收合或橫向 |
| Desktop | ≥ 1024px | 多欄、最大寬度限制（如 1200px）避免字行過長；Footer 可多欄 |

### 3.3b 交付順序補充（RWD）

視覺稿建議 **至少提供**：手機寬（375 或 390）、桌面寬（1280 或 1440）之 **首頁＋聯繫頁＋Footer**，以避免後端與前端對 Footer／QR 區塊認知不一致。

### 3.4 切版與交付順序（建議）

1. 線框（Wireframe）確認區塊與文案層級（**含手機版 Header／Footer**）。  
2. 視覺稿：**同步產出 Mobile 與 Desktop 關鍵畫面**（見 §3.3b），勿只做桌面。  
3. 標註間距、字級、色碼（或交給設計系統 Token）。  
4. 前端依 **元件** 實作，先完成 **RWD Layout + Header/Footer**，再逐頁填入。

### 3.5 設計稿尚未齊全時的作法

- 先以 **線框 + 本文之情報架構** 做靜態骨架頁。  
- 色與字級採本文「設計系統」初稿，視覺稿到後再替換 Token。

---

## 四、網站風格方向

### 4.1 關鍵字（與品牌對齊）

**專業、親民、可信、溫暖、清楚** —— 在法律服務應有的 **權威感** 與 **易於靠近、願意諮詢** 之間取得平衡；視覺與互動可朝 **現代、俐落、有生命感** 發展（見 §4.4～§4.5），但避免廉價促銷感或娛樂化。

### 4.2 視覺語彙建議

| 面向 | 建議 |
|------|------|
| **攝影／圖像** | 偏自然光、辦公或諮詢情境；避免過度 stock 握手圖。主持律師專業形象照為核心資產。 |
| **形狀** | 適度圓角（8～12px）柔化感受；卡片可略帶柔和陰影；避免過多強烈陰影或尖銳直角造成的「冰冷法庭」印象。 |
| **動效與互動** | 採 **有意義的微動效與捲動呈現**（見 §4.5），讓網站感覺 **新潮、用心**，但不能干擾閱讀或拖慢操作。 |
| **文案語氣** | 簡潔、敬體；避免過度口語或過度冷硬；法律免責與「個案不同」等提醒可放頁尾或聯絡頁小字。 |

### 4.3 字型建議（實作時再嵌入）

- **中文**：Noto Sans TC、思源黑體等無襯線，易讀、正式感足。  
- **英文**：與中文搭配之 Sans-serif；標題可略區分字重。  
- **層級**：H1 精簡；內文行高約 1.6～1.75；單行字數中文約 25～35 字為佳（視欄寬）。

### 4.4 調性：「專業」與「親民」如何並存

| 面向 | 建議作法 |
|------|----------|
| **專業** | 資訊層級清楚、用語一致；學經歷與服務分類條理分明；配色使用已定稿之 **藍系主色**（§5），避免雜色拼貼。 |
| **親民** | 首屏與本所介紹用 **短句、分段、留白**；多用「您」與明確下一步（電話、表單）；照片真實、語氣尊重但不疏離。 |
| **新潮（現代感）** | 透過 **流暢動效、捲動節奏、按鈕回饋** 傳達「這是一個當代、可靠的在線門面」，與過時靜態網頁區隔；但仍以 **內容與信任** 為主，動效為輔。 |

### 4.5 動態與互動設計準則（新潮感）

目標：訪客感到 **網站有活力、好操作**，同時維持律所應有的 **穩重**。下列為實作時建議遵守的 **動效憲法**。

**原則**

- **短、輕、可預期**：單次過渡約 **150ms～400ms**； easing 首選 **ease-out**（進場快、收尾柔），避免彈簧過頭（過萌、不莊重）。  
- **呼應使用者行為**：hover／focus／點擊有 **即時視覺回饋**（按鈕微縮放、邊框加深、陰影微變）；表單 focus 有清楚外框色（對應 `primary`／`accent-soft`）。  
- **捲動呈現（Scroll reveal）**：主要區塊（服務卡片、最新消息列表、律師預覽）可 **進入視窗時淡入或輕位移**（translateY 約 8px～16px）；列表可用 **stagger**（逐張延遲 30ms～60ms），製造層次但不拖泥帶水。  
- **導覽列**：桌面版可於向下捲動時 **略微縮高度／加陰影**；行動版選單開合附 **滑入或淡入**，並鎖定背景捲動以避免誤觸。  
- **首頁 Hero**：可允許 **極輕微** 的背景視差或慢速 Ken Burns（裁切照片時需注意檔案大小）；若使用影片背景，需 **靜音、短循環、可加載完成後再播**，並提供靜態退路。  
- **卡片與列表**：卡片 hover 可 **微上浮（translateY -2px～-4px）+ 陰影加深**；最新消息卡片可顯示 **日期或類別** 的細線動畫（底線由左伸長）作為低調亮點。  
- **載入狀態**：列表或表單送出使用 **骨架屏（skeleton）** 或 **按鈕 loading 狀態**，避免空白閃爍。  
- **避免**：全屏自動輪播過快、無法暫停的大型動畫、閃爍文字、與品牌無關的炫光粒子；這些易削弱 **專業信任**。

**技術與效能**

- 優先使用 **CSS transform／opacity** 做動畫（利於合成層、較不卡頓）；避免長時間動畫 `width`／`height`。  
- 圖片與 Lottie／SVG 動畫需 **控檔案大小**；首屏動效總量克制，以免影響 LCP。

**無障礙（必做）**

- 遵守 `prefers-reduced-motion: reduce`：**使用者要求減少動態時**，改為 **淡入淡出或直接顯示**，關閉視差、自動輪播與大範圍位移。  
- 不將 **必要資訊** 只放在必須 hover 才出現的層級（觸控裝置與鍵盤使用者需可見）。

---

## 五、色系選擇

### 5.1 律所網站色彩心理簡述

- **深藍／藏青**：信任、專業、法律常見聯想。  
- **中性灰**：背景與邊界，不搶主色。  
- **單一強調色**（如墨綠、沉穩金、勃艮第紅）：僅用於 CTA、連結、重點標示，避免整頁過花。

### 5.2 與「無憂」品牌的搭配思路

「無憂」偏 **安定、被承接**，主色可選 **偏深的藍或藍灰**；輔色可用 **低飽和綠或暖灰** 傳達溫度，與純冷灰藍微調差異。下列 **§5.3～§5.5** 已依正式 LOGO 檔案取樣落實為可實作之色票與 Token。

### 5.3 LOGO 檔案取樣說明

- **來源檔**：`images/FE445B7ADCA3002A55983B386DDB7E216CF12966.png`（黑底、六款配色）。  
- **版面**：此檔實際為 **三列 × 兩欄**（左欄自上而下三款、右欄自上而下三款）；左右欄之間有較寬黑色溝槽，若用「三等分寬度」切圖會誤切到純黑區，取樣時需依 **內容外框** 分割。  
- **方法**：於非黑色像素範圍內分割六宮格，於每格 **內縮約 12%～15%** 後統計 RGB 主色（量化間隔降噪）。下列為 **合併六款後** 仍重複出現、可作為品牌拓展的核心色；印刷／向量稿若有標準色或 Pantone，可 **以向量／印刷規範為準** 覆寫網站 Token。

### 5.4 從六款配色萃取的核心色票（HEX）

相近取樣值已合併為單一代表色，方便開發與設計共用。

| 色票角色（設計語言） | HEX | 說明 |
|----------------------|-----|------|
| **皇室藍（主藍）** | `#5789B6` | 多款 LOGO 下半或主色塊中最常出現的藍；適合作 Primary、導覽強調。 |
| **淺青藍（輔藍）** | `#89B1C5` | 上半或高光區常見的淺藍綠調；適合作漸層、輔助面、連結 hover 底色。 |
| **鼠尾草灰綠** | `#939D98` | 偏綠的中性灰；可作邊框、次要背景、分隔。 |
| **暖灰褐（Taupe）** | `#898984` | 偏暖的中性灰；可作交替區塊底、卡片邊線。 |
| **炭褐墨（字色）** | `#393934` | 深色區塊與內文首選，比純黑柔和。 |
| **青綠藍（點綴）** | `#57ACAC` | 部分款式中波浪交界／小面積出現的冷色系；**宜少量使用**（次級按鈕、圖示、極短裝飾線），避免搶主藍。 |
| **深藍（小型強調）** | `#2F4778` | 取樣中深藍與暗部交界之折衷值，對應 LOGO 右上小圓點或深色藍區；適合 Footer、深色帶、對比強調。 |

**備註**：取樣時偶見極深藍 `#02027F` 等高對比像素，多屬 **反鋸齒／細線與黑色底交界**，不建議直接整頁使用；上表 **深藍** 改以與主藍同色系、`#2F4778` 作為可閱讀、可搭白色的深色 Token。

### 5.5 網站主題 Token 建議（對應前端／Tailwind 皆可）

以下為 **建議語意化名稱與色碼**，實作時可對應為 CSS 變數（例如 `--color-primary`）。

| Token（語意） | HEX | 建議用途 |
|---------------|-----|----------|
| `primary` | `#5789B6` | 主按鈕填色、導覽目前頁、重要圖示 |
| `primary-hover` | `#456FA5` | 主按鈕 hover（於 Primary 上加深約一階） |
| `primary-active` | `#3D5F94` | 按下狀態 |
| `accent-soft` | `#89B1C5` | 次要按鈕邊框／淺底、區塊底色、連結底色 |
| `accent-teal` | `#57ACAC` | 限動區塊、社群小標等 **點綴**（勿大面積） |
| `neutral-sage` | `#939D98` | 邊框、分隔線、輔助文字 |
| `neutral-taupe` | `#898984` | 交替區塊背景（可再覆蓋透明度）、卡片邊 |
| `ink` | `#393934` | 標題與內文主色 |
| `navy-deep` | `#2F4778` | Footer、深色 Hero 遮罩、需與白字並存之帶狀區 |
| `surface` | `#FFFFFF` | 頁面主背景 |
| `surface-muted` | `#F5F6F4` | 極淺中性底（帶一點灰綠，呼應 Sage） |

**版型配置範例**

- **預設首頁**：`surface` 為底；區塊交替使用 `surface-muted` 或極淡的 `accent-soft`（約 8%～12% 透明度模擬）。  
- **Header／主要 CTA**：`primary` 字或填色 + `ink` 或白字（依對比檢查）；白字於 `#5789B6` 上請用 **大字重或足夠字級** 以符合 AA。  
- **Footer**：`navy-deep` 背景 + `surface` 或淺灰文字；社群圖示用 `accent-soft` 或單色白。

### 5.6 無障礙與可讀性

- 文字與背景對比至少 **WCAG AA**（一般內文 4.5:1）。  
- **內文請用 `ink`（`#393934`）配 `surface`**，勿長篇使用 `primary` 作為內文色。  
- 連結不只靠顏色區分，需加底線或圖示。  
- 勿僅以紅綠傳達狀態（色弱友善）。

---

## 六、使用者體驗（UX）原則

### 6.1 信任感

- **一眼可見的聯絡方式**（電話、地址）；傳真若仍有使用僅需保留勿主打。  
- **主持律師照片與真實學經歷**；避免空洞形容詞堆疊。  
- **更新年份或版本**（如「最後更新」）可小幅提升「網站仍在維護」感受。

### 6.2 降低焦慮

- 首屏或本所介紹簡短說明「初次諮詢可如何進行」（不需寫死費用，可寫「來電詢問」）。  
- 表單欄位 **少而必要**（姓名、聯絡方式、簡述需求、同意隱私權）；送出成功後顯示 **明確成功訊息**（並說明多久內回覆若律所有政策）。

### 6.3 效能與技術 UX

- 圖片 **WebP／適當尺寸**；Hero 圖優先載入。  
- 行動版 **點擊區域足夠大**（建議 ≥ 44×44px）。  
- 表單錯誤 **逐欄提示**，聚焦到第一個錯誤欄位。

### 6.4 SEO 與在地化（高雄）

- 每頁獨立 `title` / `description`；自然融入「高雄」「家事」等詞，避免堆砌。  
- **最新消息** 為前端靜態內容（見 **§8.3**）：單篇頁仍應有獨立 URL 與 meta；**Nuxt** 得以 **SSG／預渲染** 產出列表與單篇 HTML，利於索引（見 **§8.2**、專案 `nuxt.config`）。  
- 結構化資料（如 `LegalService` 或 `LocalBusiness`）可第二階段加入。

### 6.5 留言寄信與社群連結之 UX

- **寄信目的**：訪客預期「有人會回覆」—— 成功頁或 toast 應簡短說明後續（例如將於營業時間內回電／回信）。  
- **社群外連**：FB／IG／LINE 按鈕應 **新分頁開啟**，並加上 `rel="noopener noreferrer"`；圖示需有 **可辨識替代文字**（無障礙）。  
- **LINE**：若使用官方 **LINE 加入連結** 或 **LIFF**，請與律所確認使用同一官方帳號，避免多個入口造成漏訊。

### 6.6 動態效果與「新潮感」的 UX 界線

- **動效服務於理解**：捲動進場、按鈕回饋應讓使用者 **更快找到重點**（聯絡、最新消息），而非炫技。  
- **首訪與回訪**：首頁可有較完整動線；**重複瀏覽**時動效應更克制（或可記錄「已看過首屏」略過大型進場），避免長時停留使用者感到冗長。  
- **表單與信任**：送出成功／失敗使用 **明確動畫或狀態列**（勾選、抖動提示錯誤欄位），強化「系統有接收到」；避免誇張慶祝動畫（不符律所語境）。  
- **與 §4.5 呼應**：所有動態皆須支援 **減少動態** 偏好設定；自動輪播（若有）必須 **可暫停**。

---

## 七、待你決策清單（討論後勾選／填寫）

- [ ] 主色與輔色色碼定稿  
- [ ] 是否採用固定底部「電話條」（僅手機或全裝置）  
- [x] 留言表單：送出後寄信至律師信箱（**已定**：**Railway** 後端 API，見 **§8**；待選 SMTP／郵件服務商與環境變數）  
- [x] 最新消息（**已定**：**一律於前端專案維護**，見 **§8.3**）  
- [x] IG 限時動態串接（**已定**：**Railway** 後端代理 Graph API，見 **§8**；待確認 IG 帳號類型與 Meta 開發者設定）  
- [x] 聯繫頁含 FB／IG／LINE（**已定**；待提供各平台網址）  
- [ ] 主持律師照片與 Logo 定稿檔期  
- [ ] 最新消息由誰維護（律師本人／助理）、更新頻率（**發布方式**：修改前端並重新部署 Cloudflare Pages，見 **§8**）  
- [ ] 表單收件信箱是否與 `intruduction.md` 之 Gmail 相同或另設事務所信箱  
- [ ] 提供 **LINE 官方帳號 QR Code** 清晰圖檔（頁尾與必要時聯繫頁並用）  
- [ ] 首頁 Hero 是否採用 **影片／視差／Ken Burns**（涉及檔案與無障礙，見 §4.5）

---

## 八、開發與部署架構

本節為 **已定案** 之技術與託管分工：前端與後端分離；前端為 **Nuxt 3**（見 **§8.2**、**§8.7**）；後端程式語言為 **Node.js**（見 **§8.4**）；**後端僅承擔「表單寄信」與「Instagram 限時動態 API 代理」**，不含資料庫 CMS。**不使用 Supabase**（無另行宣告需求前，不引入資料庫／後台登入等元件）。

### 8.1 託管與預估費用

| 項目 | 角色 | 預估費用 |
|------|------|----------|
| **Cloudflare Pages** | 託管 **Nuxt** 建置產物、全球 CDN、HTTPS、自訂網域 | **免費**方案為主（依官方方案調整） |
| **Railway** | 單一 **Node.js** 後端：**聯絡表單寄信**、**IG Graph API 代理**（Secret／長效權杖僅存後端） | 約 **NT$150／月**（依官方方案與匯率浮動） |
| **Supabase** | **本專案不採用** | — |
| **網域** | 建議 DNS 交 **Cloudflare** 管理，與 Pages／子網域整合較單純 | 約 **NT$300～500／年** |

### 8.2 前端框架與建置

- **框架**：**Nuxt 3**（**Vue 3** + **TypeScript**）。建置底層為 **Vite**；頁面路由、SEO head、靜態輸出等由 Nuxt 統一管理，利於 **§6.4** 與最新消息頁。  
- **部署**：**Cloudflare Pages**（連線 Git 建置；建置 preset／輸出模式須與 Pages [官方支援之 Nuxt 部署方式](https://developers.cloudflare.com/pages/framework-guides/deploy-a-nuxt-site/) 相容，以 **靜態／SSG 為主**）。  
- **環境變數**：後端 API 基底網址使用 **Nuxt runtime 公開設定**（例如 `NUXT_PUBLIC_API_BASE` 等命名，依實作為準），**不得** 將 Meta App Secret 或郵件密碼置於前端。  
- **設計系統與 UI 套件**：見 **§8.7**（與 **§4**、**§5** 呼應）。

### 8.3 內容：最新消息

- **最新消息**（列表與各單篇）**一律寫在前端專案內**（例如集中於 `news` 資料模組、Markdown 或 JSON，由實作選定）。  
- **發布或修改**一則消息＝**更新 repo 並重新建置、部署 Cloudflare Pages**；無後台資料庫。

### 8.4 後端（Railway）

- **單一程式／單一服務**，**運行環境定案為 Node.js**（HTTP 框架如 **Express**、**Fastify**、**Hono** 等由實作選定），**職責僅下列兩項**：  
  1. **POST 聯絡表單**：驗證欄位、honeypot／限流／（選）reCAPTCHA；經 **SMTP 或 Resend／SendGrid 等交易型郵件 API** 寄至律師指定信箱。  
  2. **GET（或專用路由）IG 限時動態**：伺服器端呼叫 **Instagram Graph API**，可附簡短快取；回傳 JSON 供前端渲染；失敗時前端依 **§2.6** 顯示備援。  
- **CORS**：僅允許 **官網網域**（含 Cloudflare Pages 預覽網域若需要）。  
- **設定**：API Base URL、SMTP／郵件服務、Meta 相關環境變數皆於 **Railway** 設定，**不可** 提交至 Git。

### 8.5 網域與路由建議

- **官網**（`www` 或 apex）→ **Cloudflare Pages**。  
- **API**（例如 `api.網域.com`）→ **Railway** 服務（實際子網域於部署時定案）。  
- 前端呼叫 API 時使用 **HTTPS**；正式環境與預覽環境的 **CORS** 需分別設定或沿用白名單。

### 8.6 與其他章節之對應

- 表單與法遵、備援文案：**§2.5**、**§6.5**。  
- IG 產品條件與過渡方案：**§2.6**。  
- 最新消息 SEO：**§6.4**。  
- 視覺調性、動效憲法、色票 Token：**§4**、**§5**；元件與動效實作細節：**§8.7**。

### 8.7 前端設計系統與 UI 套件（已定）

下列與他案討論之「律所官網／精品企業」方向對齊本文件 **§4.1～§4.5**、**§5**，並作為實作準則；**全站資訊架構仍以 §2.2 為準**（未列入 §2.2 之頁面—如見證、案例長列表—不視為本階段必做，除非另開需求）。

**核心技術**

| 類別 | 選用 | 說明 |
|------|------|------|
| 樣式 | **Tailwind CSS** | Utility-first；與 **§5 Token** 對應為 `tailwind.config`／CSS 變數，避免散落 magic number；避免大量 inline style。 |
| 元件庫 | **Nuxt UI** | 表單、按鈕、卡片、導覽、抽屜、Modal、Dropdown、Tabs、Accordion、Alert、Toast 等；**須以品牌色與圓角／陰影客製**，避免預設「通用後台」感。 |
| 圖示 | **Lucide**（outline 為主） | 用量克制；避免嬉戲感或過粗線條。 |
| 進階動效（已定） | **GSAP** | **全站進階動效以此為準**（必要時可搭配 **ScrollTrigger**，仍須克制）；適用：淡入、極輕位移、區塊進場、文字／數字 reveal、極輕視差等。**一般微互動**（hover、focus）仍以 **CSS** 為主即可，無須全部綁 GSAP。動效 **必須**與 **§4.5**、`prefers-reduced-motion` 一致；禁止彈跳、過快或炫技捲動。 |

動效分工簡述：**CSS** 處理按鈕／連結／卡片的即時回饋；**GSAP** 處理進場、捲動揭露等需要時間軸或較細控制的段落（見 **§3.2b**、**§4.5**）。

**無樣式／可及性補強**

- **優先**使用 **Nuxt UI** 已涵蓋之鍵盤操作與語意。  
- 若遇 **Nuxt UI 未涵蓋之互動**，再評估 **Headless UI（Vue）** 等補位，**避免** 同時堆疊多套完整無樣式庫造成體積與行為不一致。

**版型與元件氣質（呼應 §4）**

- **整體**：精品企業、編輯感、留白充足、層級清楚；律所應有的 **穩重、可信、溫暖**，避免新創 Landing、SaaS 儀表板、科技炫光、重型玻璃擬態、卡通互動。  
- **按鈕**：適度圓角、清楚 hover／focus；避免霓虹、誇張漸層。  
- **卡片**：薄邊框、柔和陰影、內距寬鬆；避免厚重玻璃模糊疊層。  
- **表單**：標籤清楚、觸控區與 **§6.3** 一致；錯誤態與焦點樣式對齊 **§5** `primary`／`accent-soft`。

**響應式與無障礙**

- **Mobile-first**、與 **§3.3** 斷點一致；避免過小按鈕與橫向強迫捲動長文。  
- 對照 **§5.6**、**§4.5** 無障礙段落；語意化 HTML、標題階層、對比與表單可及性。

**給實作／AI 輔寫之簡律**

- 一致性優於炫技；動效 **短、輕、可預期**（§4.5）。  
- 維持「**可親近的專業律所品牌**」形象，避免 AI 產品站或政府公文網的極端。

---

## 九、文件修訂

| 版本 | 日期 | 修訂摘要 |
|------|------|----------|
| 0.1 | 初稿 | 建立操作流程、切版、風格、色系、UX 框架 |
| 0.2 | — | 納入與律師討論之六項功能：本所介紹、表單寄信、律師介紹、最新消息、IG 限時、聯繫頁與社群 |
| 0.3 | — | 依 LOGO PNG 取樣萃取 HEX，並定義網站主題 Token（§5.3～§5.5） |
| 0.4 | — | 補充「專業／親民」調性、新潮動效準則（§4.4～§4.5）、動態 UX 界線（§6.6）、切版動效備註（§3.2b） |
| 0.5 | — | 明定 RWD 為必備；頂部 LOGO＋導覽、頁尾聯絡／重複導覽／社群／LINE QR 規格（§2.0、§2.2b、§3.3～§3.3b） |
| 0.6 | — | 新增開發與部署架構（§8）：Vue＋Cloudflare Pages、Railway 單一後端（寄信＋IG）、最新消息前端維護；不使用 Supabase |
| 0.7 | — | 後端程式語言定案為 **Node.js**（§8、§8.4） |
| 0.8 | — | 前端定案 **Nuxt 3 + TypeScript**；Tailwind、Nuxt UI、Lucide；§8.7 設計系統與 IA 對齊說明 |
| 0.9 | — | 進階動效庫定案為 **GSAP**（§8.7）；輕量使用並遵守 §4.5、`prefers-reduced-motion` |

---

*本文件依 `intruduction.md` 之事務所資訊撰寫，定稿前請與主持律師確認所有對外文案與法遵表述。*
